# @swiss-now/motion

Shared visual and motion system for both rendering targets (interactive web, Remotion video).
Content arrives in the Phase 0 design sprint: tokens, scales, deterministic animation math,
stateless SVG primitives taking `progress ∈ [0,1]`, and scene specs. See `docs/MOTION_SYSTEM.md`.
