# @swiss-now/geo-build

Build-time only. Converts swisstopo, SBB and BAV geodata (LV95) into web-ready WGS84 GeoJSON
and route paths. Requires GDAL (`ogr2ogr`) and mapshaper. Not part of the runtime bundle.
See `docs/ARCHITECTURE.md` §4 and `docs/DATA_SOURCES.md` §12.
