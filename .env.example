# Swiss Now — environment variables (none required in Phase 0; all sources are open)
# OTD_API_KEY=            # opentransportdata.swiss (Phase 2, rail)
# ENTSOE_TOKEN=           # ENTSO-E Transparency Platform (Phase 5, energy)
# BLOB_READ_WRITE_TOKEN=  # Vercel Blob (Phase 2+, Mode 2 outputs)
